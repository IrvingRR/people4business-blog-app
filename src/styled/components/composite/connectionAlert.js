import styled from "styled-components";

export const ConnectionAlertWrapper = styled.div`
    width: 100%;
    padding: 10px 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    background-color: var(--primary-color);
    position: absolute;

    svg { 
        font-size: var(--fs-xl);
    }
`;